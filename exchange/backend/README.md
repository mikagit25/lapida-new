# Lapida Exchange Backend

Node.js/Express API для аналитики, истории обменов, прокси для DEX.

## Основные эндпоинты
- /api/exchange — история обменов
- /api/analytics — аналитика
- /api/proxy — прокси для DEX

## Старт разработки
1. npm install
2. npm start
